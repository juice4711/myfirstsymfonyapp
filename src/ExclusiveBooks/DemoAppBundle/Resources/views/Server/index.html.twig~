{% extends '::base.html.twig' %}

{% block body -%}
	<div id="clear">
		{% for flashMessage in app.session.flashbag.get('notice') %}
		<div class="flash-notice">
			{{ flashMessage }}
		</div>
		{% endfor %}
	</div>
	<div id="leftSide">
		<fieldset>
		<legend>Server List</legend>

			<table class='rightTable'>
			{% for entity in entities %}
				<tr class="{{ cycle(['orderlist highlight','orderlist'],loop.index) }}" ">
					<td class='orderlisttd'>{{ entity.serverName }}</td>
					<td class='orderlisttd'>{{ entity.serverLocation }}</td>
					<td class='orderlisttd'>{{ entity.serverIP }}</td>
					<td class='orderlisttd'><a href="{{ path('admin_server_show', { 'id': entity.id }) }}">show</a></td>
					<td class='orderlisttd'><a href="{{ path('admin_server_edit', { 'id': entity.id }) }}">edit</a></td>
				</tr>
			{% endfor %}
			</table>
		</fieldset>
	</div>

	{{ render(controller('ExclusiveBooksDemoAppBundle:Admin:mainmenu')) }}

    {% endblock %}
