{% extends '::base.html.twig' %}

{% block body -%}
  <div id="clear">
		{% for flashMessage in app.session.flashbag.get('notice') %}
		<div class="flash-notice">
			{{ flashMessage }}
		</div>
		{% endfor %}
	{{ form_start(form) }}
	<p>
		{{ form_errors(form.smsText) }}
		{{ form_errors(form) }}
	</p>
	</div>
	<div id="leftSide">
		<fieldset>
		<legend>Send SMS</legend>
		
		{{ form_label(form.smsText, 'SMS Text') }}
		<div class="div_texbox">{{ form_widget(form.smsText) }}</div>
		
		<div class="button_div">{{ form_widget(form.submit) }}</div>
		</fieldset>
	</div>
{{ form_end(form) }}
	{{ render(controller('ExclusiveBooksDemoAppBundle:Admin:mainmenu')) }}

    {% endblock %}

