{% extends '::base.html.twig' %}

 {% block body %}
     Welcome to the Admin Interface for the Dashboard.
<p>
{% for flashMessage in app.session.flashbag.get('notice') %}
    <div class="flash-notice">
        {{ flashMessage }}
    </div>
{% endfor %}
<p><a href="{{ path('addserver') }}">
  Setup a Server.
</a>
 {% endblock %}

